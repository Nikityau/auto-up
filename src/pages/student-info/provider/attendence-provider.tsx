import React from 'react';

const AttendenceProvider = ({children}:React.PropsWithChildren) => {
    return (
        <>
            {children}
        </>
    );
};

export default AttendenceProvider;