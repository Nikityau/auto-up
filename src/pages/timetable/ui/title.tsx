import React from 'react';

const TimetableTitle = () => {
    return (
        <div className='timetable-page__title'>
            <span>Расписание</span>
        </div>
    );
};

export default TimetableTitle;